#include <stdlib.h>
#include <stdio.h>
/**
 *main - Prints its name
 *@argc: Argument counter
 *@argv: Argumnet array
 *Return: Always (0)
 */

int main(int argc, char **argv)
{
	printf("program name: %s \n", argv[0]);

	return (0);
}
