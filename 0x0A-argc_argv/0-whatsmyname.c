#include <stdlib.h>
#include <stdio.h>
/**
 *main - Prints its name
 *@argc: Argument counter
 *@argv: Argumnet array
 *Return: Always (0)
 */

int main(int argc __attribute__((unused)), char **argv)
{
	printf("%s \n", argv[0]);

	return (0);
}
