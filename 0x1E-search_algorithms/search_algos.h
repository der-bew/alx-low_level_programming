#ifndef _SEARCHALG_H_
#define _SEARCHALG_H_

#include <stdio.h>
#include <stdlib.h>

void print_array(int *array, size_t size);
int linear_search(int *array, size_t size, int value);
int binary_search(int *array, size_t size, int value);


#endif /*_SEARCHALG_H_ */
